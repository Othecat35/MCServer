A file where I dumped ideas for this project.

CLI Commands:
- Add `init` and `install`
  `init` is used initialize the ".mcserver" directory and its configurations.
  And `install` is used to install and setup the Minecraft server with the configuration inside ".mcserver".

Mod Management:
- Add mod search command with Modrinth: Done!
- Mod search with key=value. eg, mcserver search lithium limit=20: using flag

Miscellaneous
- Rename "ram" to "memory"? Only in script
- Shrink the project scope first: Yep!

new things
- Create config generator and loader: Done
- Make the search_mod limit based in modrinth configuration file: Done

Here's where I can yap about something
- CHANGELOGS.md is primarily for myself?

---

Semantic Versioning (MAJOR.Minor.patch):
- MAJOR: When you make incompatible changes
- Minor: When you add or change functionality that is backward compatible
- patch: When you make backward compatible bug fixes

The "v" Version Prefix:
- Use in:
  - Git Tag
  - Github Release
  - `mcserver --version`
  - User Agent
  - (Basically everything that a user will see)
- Don't use in:
  - Internal script version (`__version__`)
  - Changelog header
  - (Basically everything that is internal about the project)

Changelog Structure (Examples):
# Changelog
All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Mod indexing

### Removed
- The whole project

## [<semver>] - <date(YYYY-MM-DD)>

### Added
- New command `D`

### Changed
- Command `A` now print `Hello, world!`

### Deprecating
- Removing command `C`

### Removed
- Command `B`

### Fixed
- Bug where the script stuck in an infinite loop

### Security
- Fix auth bug

[unreleased] : https://github.com/Othecat35/MCServer/compare/<latest_tag>...HEAD
[<current_tag>] : https://github.com/Othecat35/MCServer/compare/<prev_tag>...<current_tag>

---
dependencies and dependents
mod indexing
split mod dependency resolution and download from the add_mod command: done, but just the BFS
make it nicer (imposible)
- what am I doing making this project? my school has nothing to do with it. Me being self-deprecating and having Impostor Syndrome while coding is apparently normal? how?
paper? later.
progress bar: whats that
more efficient sha calculating directly while downloading instead if after downloading: yay
warn client only mod because as the name suggest, it is designed for server! client already have too much attention ;/
fabric installer, be dynamic (but sadly coding is not that magical that I can command the computer easily :insert_smile_with_tears_emoji:)
mod indexing is per mod. dependencies and depends, is a list, which contains id and type.
move the print_status as a callback to intergrate directly in resolve_mods: done
download_url with a status indicator like how much is downloaded vs total seems great: there, check it yourself
add init options for search because I felt like it
Lol the data-driven backup >:(
pid lock I guess
java installed check smh: fine, there
be a good API citizen and implement ratelimit check >:)
auto download sometimes, when there's no optional or incompatible mods, or the size is "reasonable"
haha, the scope will grow even more when you implement that rdepends >:)
oh hell nah, I already implemented that rdepends? how so quick? I have AI as helper, if you know rubberducking, that is exactly what I'm doing! not vibecodinf I swear.
Before that, fix my data migration logic first to make every single entry uses id as the key and don't leave any slug left!
After that, also implement a conflict check pls, like if you request this mod and that mod, and they met as "incompatible", whatever. you are kinda responsible for that, not really but nice to have, and make yourself comoareable to Prism >:). by "you" I mean myself.
And then? hahaha, the hole of the rabbithole for dependency resolution didn't end there, you have to index them. why? because I said so. nah jk, because we should know that we already have Fabric API installed so we don't redownload it again
remove? list? update? clean? check? yep, btw clean means remove orphaned mods, and check? I guess that can be used to scan for any manual .jar added in mods/
now make any mod that are incompatible ripples down to the source, only stopped by an optional mod -_-, wait, why I'm bother to fetch optional mods? just make it required only!
You screwed up the add command lol
